//Utility

function robogerInteger(input) {
    const integer = input.toString();
    if (integer === "1" || integer === "3" || integer === "5") {
        return true;
    } else {
        return false;
    }
}

//Business Logic